/* THE BASICS 
1)	SELECT (column name and can use aggregation here and ALIAS (AS) with aggregated column)
2)	DISTINCT (retrieve unique value)
3)	FROM (Table name)
4)	WHERE (use with OR, AND & logical operators such as IN, LIKE, ILIKE, NOT LIKE, BETWEEN, =, <>, >, <, >=, <=)
5)	GROUP BY (use GROUP BY with single or multiple aggregation functions like COUNT, SUM, AVG, MIN, MAX)
6)	HAVING (filter aggerated output)
7)	ORDER BY (sort the result either by ASC or DESC)  
8)	LIMIT (set limit on number of rows returned)
SHOW DATABASES;
USE retaildata;
SHOW TABLES;
SHOW COLUMNS FROM brandscsvimport;
SHOW INDEXES FROM brandscsvimport;
SHOW STATUS;
SHOW VARIABLES;
*/